<?php
/**
 * Configuration template — copy to config.php and fill in real values.
 * config.php is gitignored so each environment (local, production) keeps its own.
 */

define('DB_HOST', 'localhost');
define('DB_NAME', 'your_db_name');
define('DB_USER', 'your_db_user');
define('DB_PASS', 'your_db_password');

// Game origin for CORS — use '*' for dev, lock to your domain in production
define('GAME_ORIGIN', '*');
